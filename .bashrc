# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions
export SCREENDIR=~/.screen

export RSENSE_HOME=$HOME/local/lib/rsense-0.3

export TERM=xterm-256color

